{
  "name": "with-redux-toolkit",
  "version": "1.0.0",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider --no-warnings && set NODE_TLS_REJECT_UNAUTHORIZED=0 && next dev",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && next build",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && next start"
  },
  "dependencies": {
    "@heroicons/react": "^1.0.6",
    "@reduxjs/toolkit": "^1.9.5",
    "@tailwindcss/line-clamp": "^0.2.0",
    "firebase": "^8.6.1",
    "firebase-admin": "^9.8.0",
    "next": "^14.2.0",
    "next-auth": "^4.24.11",
    "react": "^18.2.0",
    "react-currency-formatter": "^1.1.0",
    "react-dom": "^18.2.0",
    "react-redux": "^8.1.1",
    "react-responsive-carousel": "^3.2.23"
  },
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.15",
    "tailwindcss": "^2.1.2"
  }
}
